export const metadata = {
    title: 'Đăng kí | Mixi Shop',
    description: 'Trang đăng kí của MixiShop.',
}

export default function DashboardLayouts({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <>
            {children}
        </>
    )
}
